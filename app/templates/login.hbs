{{header-navbar title="Login"}}

<div class="content">
  <div class="box">
    <form>
      {{input type="text" placeholder="Phone Number" value=phone}}
      {{input type="password" placeholder="Password" value=password  }}
      <button {{action 'login' }} class="btn btn-positive btn-block">Choose existing</button>
    </form>
  </div>
</div>

